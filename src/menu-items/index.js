// project import
import utilities from './support';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [utilities]
};

export default menuItems;
